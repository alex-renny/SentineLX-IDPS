import jwt from "jsonwebtoken";
const secret = () => process.env.SENTINELX_JWT_SECRET || process.env.JWT_SECRET || "";
export function authenticate(req, res, next) { const value = req.headers.authorization || ""; const token = value.startsWith("Bearer ") ? value.slice(7) : ""; if (!secret() || secret().length < 32 || !token) return res.status(401).json({ success: false, message: "Authentication required" }); try { req.user = jwt.verify(token, secret(), { algorithms: ["HS256"], issuer: "sentinelx-idps", audience: "sentinelx-ui" }); return next(); } catch { return res.status(401).json({ success: false, message: "Invalid or expired session" }); } }
export function requireAdmin(req, res, next) { return req.user?.role === "admin" ? next() : res.status(403).json({ success: false, message: "Administrator access required" }); }
export function verifySocket(socket, next) { try { const token = socket.handshake.auth?.token; if (!secret() || secret().length < 32 || !token) throw new Error("Unauthorized"); socket.user = jwt.verify(token, secret(), { algorithms: ["HS256"], issuer: "sentinelx-idps", audience: "sentinelx-ui" }); next(); } catch { next(new Error("Unauthorized")); } }
