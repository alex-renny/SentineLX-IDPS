export function allowMethods(req, res, next) { if (!["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"].includes(req.method)) return res.status(405).json({ success: false, message: "Method not allowed" }); next(); }
export function rejectMongoOperators(req, res, next) { const unsafe = (value) => { if (!value || typeof value !== "object") return false; return Object.entries(value).some(([key, item]) => key.startsWith("$") || key.includes(".") || unsafe(item)); }; if (unsafe(req.body) || unsafe(req.query) || unsafe(req.params)) return res.status(400).json({ success: false, message: "Invalid request input" }); next(); }
